---
"slidev-addon-fancy-arrow": minor
---

Add shorter `duration` and `delay` props as alternatives to `animationDuration` and `animationDelay`. The original props are deprecated but still supported for backward compatibility.
